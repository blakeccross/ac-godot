class_name SpawnPoint
extends Resource

## Named spawn on the cell grid (player, later visitors).

@export var id: StringName = &"player"
@export var cell: Vector2i = Vector2i.ZERO
@export var yaw: float = 0.0
